[Unit]
Description=Run the ZimaOS server-side backup daily

[Timer]
OnCalendar=*-*-* 04:00:00
# If the box was off or rebooting at 04:00, run as soon as it's back up
# instead of waiting a full day.
Persistent=true
RandomizedDelaySec=120

[Install]
WantedBy=timers.target
